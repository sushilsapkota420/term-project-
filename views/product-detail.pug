extends layout

block content
  h2= box.name
  .card
    p
      strong ID:
      |  #{box.id}
    p
      strong Theme:
      |  #{box.theme}
    p
      strong Price:
      |  $#{box.price}
    p
      strong Stock:
      |  #{box.stock}
    a.button(href="/products") Back to Products