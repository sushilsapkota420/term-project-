extends layout

block content
  section.message-card
    span.section-tag Error
    h2 404 — Not Found
    p Sorry, we could not find "#{identifier}".
    p Try browsing the full product catalog instead.
    a.btn.btn-primary(href="/products") Return to Products